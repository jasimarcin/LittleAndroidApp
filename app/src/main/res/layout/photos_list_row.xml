<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <variable
            name="viewModel"
            type="com.marcin.jasi.littleandroidapp.photosList.presentation.viewModel.PhotosListItemViewModel" />
    </data>

    <android.support.constraint.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_margin="@dimen/photos_list_image_margin"
        android:layout_height="wrap_content"
        android:background="@color/photos_list_row_background_color">

        <ImageView
            android:layout_width="@dimen/photos_list_image_size"
            android:layout_height="@dimen/photos_list_image_size"
            android:layout_margin="@dimen/photos_list_image_margin"
            app:imageUrl="@{viewModel.url}"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            tools:background="#000" />

        <TextView
            android:id="@+id/item_number"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintRight_toRightOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            android:text="@{viewModel.title}"
            tools:text="Item: 4" />

    </android.support.constraint.ConstraintLayout>

</layout>
